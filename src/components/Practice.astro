---
/*
  PRACTICE SECTION - ARUNA

  Placeholder para imagen (opcional): practice-image.jpg
  Idea: Detalle de manos en mudra sobre las rodillas. O pies descalzos
  sobre el mat. O una taza de te junto a bloques de yoga y una manta.
  Angulo cerrado, intimidad.
  Transmite: "los pequenos detalles importan, esto es autocuidado"

  Especificaciones:
  - Formato: JPG o WebP
  - Resolucion: 1920px ancho minimo
  - Estilo: luz natural, tonos calidos, detalle intimo
*/

const horarios = [
  { dia: 'Lunes', hora: '10:00 - 11:15' },
  { dia: 'Miercoles', hora: '10:00 - 11:15' },
  { dia: 'Viernes', hora: '10:00 - 11:15' },
  { dia: 'Sabado', hora: '09:30 - 10:45' },
]
---

<section id="practica" class="py-24 md:py-32 bg-beige-claro/30">
  <div class="max-w-6xl mx-auto px-6">
    <div class="grid md:grid-cols-2 gap-12 md:gap-16 items-start">
      <!-- Text Content -->
      <div class="space-y-6 stagger">
        <h2 class="fade-up text-3xl md:text-4xl text-stone font-light">
          Vinyasa Yoga
        </h2>

        <div class="fade-up space-y-4 text-stone-light leading-relaxed">
          <p>
            Una practica fluida que conecta movimiento y respiracion. No
            necesitas experiencia previa ni flexibilidad. Solo necesitas llegar.
          </p>

          <p>
            En ARUNA adaptamos cada clase a quienes estan presentes. Hay espacio
            para todas: para la que viene por primera vez, para la que lleva
            anios practicando, para la que hoy solo necesita descansar.
          </p>
        </div>

        <!-- Schedule -->
        <div class="fade-up pt-6">
          <h3 class="text-lg text-stone mb-4 font-medium">Horarios</h3>
          <div class="space-y-3">
            {horarios.map(({ dia, hora }) => (
              <div class="flex justify-between items-center py-2 border-b border-beige-medio/30">
                <span class="text-stone">{dia}</span>
                <span class="text-stone-light">{hora}</span>
              </div>
            ))}
          </div>
          <p class="text-sm text-beige-oscuro mt-4 italic">
            Los horarios pueden variar. Escribenos para confirmar disponibilidad.
          </p>
        </div>
      </div>

      <!-- Image (optional) -->
      <div class="fade-up">
        <div class="relative aspect-square bg-beige-claro rounded-sm overflow-hidden">
          <!-- Placeholder: reemplazar con imagen real -->
          <div class="absolute inset-0 flex items-center justify-center text-beige-oscuro/50">
            <div class="text-center p-8">
              <svg class="w-16 h-16 mx-auto mb-4 opacity-50" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
              <p class="text-sm">practice-image.jpg</p>
              <p class="text-xs mt-2 opacity-70">Manos en mudra, mat, detalles</p>
            </div>
          </div>

          <!-- Cuando tengas la imagen, reemplaza el div anterior con: -->
          <!--
          <img
            src="/practice-image.jpg"
            alt="Detalle de practica de yoga"
            class="absolute inset-0 w-full h-full object-cover"
          />
          -->
        </div>
      </div>
    </div>
  </div>
</section>
